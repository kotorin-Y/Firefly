---
const { metrics } = Astro.props as { metrics: Array<{ value: string; label: string }> };
---
<div class="project-metrics">{metrics.map((metric) => <div><strong>{metric.value}</strong><span>{metric.label}</span></div>)}</div>
